# 1. 이름을 Cloudflare 대시보드에 생성된 이름("my-telegram-bot")과 일치시키세요.
name = "my-telegram-bot"

# 2. 파일 경로가 정확한지 확인하세요. (src 폴더 안에 entry.py가 있는 경우)
main = "src/entry.py"

# 3. Python 실행을 위해 매우 중요한 설정입니다. 날짜를 최신으로 유지하세요.
compatibility_date = "2025-12-01" 
compatibility_flags = ["python_workers"]